# nodejs_azureFunction
Azure Function Created in Node
Pass a name in the url in the parameter and it will return Hello plus the name
https://domnodefunction.azurewebsites.net/api/HttpTriggerJs?name=Jerry
